import React, { Fragment } from 'react';

const landing = () => {
  return (
    <Fragment>
      <h2>This is a landing page.</h2>
      <p>
        <em>Sign up or in with the buttons on the top right.</em>
      </p>
    </Fragment>
  );
};

export default landing;
